import style from "../css/Header.module.css"
function Header(){
    return(
        <header>
            <nav>
               <a href="/">Add Pokemon</a>
            </nav>
        </header>
    );
}

export default Header;